---
const featuresBorrower = [
  "Reach real people willing to lend, not only institutions.",
  "Borrow in crypto or stablecoins — directly wallet to wallet.",
  "Use your own wallets — including hardware wallets like Ledger or Trezor.",
  "Keep full control of your coins — Amicbridge never holds funds or keys.",
  "Explain your situation in your own words.",
  "Negotiate repayment terms and interest directly with lenders.",
  "Whenever possible, we match you with manually verified lenders."
];

const featuresLender = [
  "See real people and projects, not just numbers.",
  "Lend using crypto or stablecoins — directly wallet to wallet.",
  "Use any wallet you prefer (exchange, software, or hardware wallets).",
  "Earn interest by lending directly to borrowers — you choose the rate.",
  "You stay in control — Amicbridge never stores or holds your coins or private keys.",
  "Set your own interest rate and conditions.",
  "Chat with borrowers before you commit.",
  "Apply for a Verified lender badge after a quick manual review."
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Amicbridge – Peer-to-Peer Crypto Lending</title>

    <meta
      name="description"
      content="Amicbridge connects crypto borrowers and lenders directly — wallet-to-wallet, fully non-custodial."
    />

    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <style>
      :root {
        color-scheme: dark;
        --bg: #020617;
        --card: #020617;
        --accent: #22c55e;
        --accent-soft: #064e3b;
        --text: #e5e7eb;
        --muted: #9ca3af;
        --border: #1f2933;
      }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        background: radial-gradient(circle at top, #0f172a 0, #020617 55%);
        color: var(--text);
      }

      .shell {
        max-width: 1000px;
        margin: 0 auto;
        padding: 2.5rem 1.5rem 4rem;
      }

      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
      }

      .logo {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        font-weight: 600;
        letter-spacing: 0.04em;
      }

      .logo-mark {
        width: 26px;
        height: 26px;
        border-radius: 999px;
        background: radial-gradient(circle at 30% 20%, #4ade80, #16a34a);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.9rem;
        color: #022c22;
      }

      nav a {
        font-size: 0.9rem;
        color: var(--muted);
        text-decoration: none;
        margin-left: 1.2rem;
      }

      .btn {
        border-radius: 999px;
        padding: 0.6rem 1.4rem;
        border: 1px solid transparent;
        font-size: 0.95rem;
        font-weight: 500;
        cursor: pointer;
        background: transparent;
        color: var(--text);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        justify-content: center;
      }

      .btn-primary {
        background: var(--accent);
        color: #022c22;
      }

      .btn-outline {
        border-color: var(--border);
      }

      .hero {
        margin-top: 1rem;
        margin-bottom: 2.5rem;
      }

      h1 {
        font-size: clamp(2.3rem, 3vw + 1.2rem, 3.1rem);
        line-height: 1.05;
        margin-bottom: 1rem;
      }

      h1 span {
        color: var(--accent);
      }

      .hero-text {
        font-size: 0.98rem;
        color: var(--muted);
        max-width: 32rem;
        margin-bottom: 1.5rem;
      }

      .cta-row {
        display: flex;
        flex-wrap: wrap;
        gap: 0.8rem;
        margin-bottom: 1.2rem;
      }

      .tiny-note {
        font-size: 0.78rem;
        color: var(--muted);
      }

      .features {
        display: grid;
        gap: 1rem;
        margin-top: 3rem;
      }

      @media (min-width: 800px) {
        .features {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }

      .feature-card {
        border-radius: 1rem;
        border: 1px solid rgba(148, 163, 184, 0.25);
        background: radial-gradient(circle at top left, #0f172a, #020617);
        padding: 1.2rem 1.3rem 1.1rem;
      }

      .feature-card h2 {
        font-size: 1.1rem;
        margin-bottom: 0.4rem;
      }

      .feature-card ul {
        padding-left: 1.1rem;
        margin: 0.3rem 0 0.5rem;
        font-size: 0.86rem;
        color: var(--muted);
      }

      .feature-card li + li {
        margin-top: 0.25rem;
      }

      footer {
        margin-top: 2.5rem;
        font-size: 0.75rem;
        color: var(--muted);
        border-top: 1px solid rgba(30, 64, 175, 0.3);
        padding-top: 1rem;
      }
    </style>
  </head>

  <body>
    <div class="shell">
      <header>
        <div class="logo">
          <div class="logo-mark">A</div>
          <span>Amicbridge</span>
        </div>
        <nav>
          <a href="#how">How it works</a>
          <a href="/verify-wallet" class="btn btn-outline">Verify wallet</a>
        </nav>
      </header>

      <main>
        <section class="hero">
          <h1>
            Borrow crypto. <span>Lend crypto.</span><br />
            Wallet to wallet. No custodian.
          </h1>
          <p class="hero-text">
            Amicbridge connects people who want to borrow with people who want to lend —
            using crypto and stablecoins only. Non-custodial, wallet-to-wallet,
            and focused on transparency and trust.
          </p>

          <div class="cta-row">
            <a href="/verify-wallet" class="btn btn-primary">
              Connect / verify wallet
            </a>
            <a href="#how" class="btn btn-outline">
              Learn how it works
            </a>
          </div>

          <p class="tiny-note">
            We never hold your funds or private keys. All payments happen directly
            between your wallets.
          </p>
        </section>

        <section id="how" class="features">
          <div class="feature-card">
            <h2>For borrowers</h2>
            <ul>
              {featuresBorrower.map((f) => <li>{f}</li>)}
            </ul>
          </div>

          <div class="feature-card">
            <h2>For lenders</h2>
            <ul>
              {featuresLender.map((f) => <li>{f}</li>)}
            </ul>
          </div>
        </section>

        <footer>
          <p>
            <strong>Important:</strong> Amicbridge is a connection platform.
            We never hold funds, private keys, or custody. All payments and agreements
            happen directly between wallets. Always verify who you are dealing with
            before sending any crypto.
          </p>
        </footer>
      </main>
    </div>
  </body>
</html>
